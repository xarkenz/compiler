use super::*;

#[test]
fn test_format_sizes() {
    // use gen::info::*;
    // 
    // let symbol_table = SymbolTable::new(16);
    // 
    // assert_eq!(Format::Never.size(&symbol_table), Some(0));
    // assert_eq!(Format::Void.size(&symbol_table), Some(0));
    // assert_eq!(Format::Boolean.size(&symbol_table), Some(1));
    // assert_eq!(Format::Integer { size: 4, signed: true }.size(&symbol_table), Some(4));
    // assert_eq!(Format::Void.into_pointer(PointerSemantics::Immutable).size(&symbol_table), Some(std::mem::size_of::<*const ()>()));
}